# flutter_print_platform_interface

A common platform interface for the [flutter_print](https://pub.dev/packages/flutter_print) plugin.

This interface allows platform-specific implementations of the flutter_print plugin, as well as the plugin itself, to ensure they are supporting the same interface.
